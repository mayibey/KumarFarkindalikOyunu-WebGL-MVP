*.asset filter=lfs diff=lfs merge=lfs -text
*.unityweb filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text

# Builds/ altındaki tüm dosyalar normal git'te (LFS dışı) — wasm/data dahil
Builds/** -filter -diff -merge -text
Builds/** !text !filter !merge !diff
